// Overrides for admin.js — loaded after admin.js.
// TODO (next session): override window.saveUser/deleteUser/openUserForm to
// call /api/v1/users CRUD endpoints instead of mutating window.PEOPLE, and
// bypass assignMockRoles() since real users already carry role_id from D1.
// Placeholder no-op for now.
(function () {})();
